import * as mongoose from "mongoose";
const mongooseSch=new mongoose.Schema({
    "collectionName" : {type:String,default:''},
    "count" : {type:Number,default:0},
});
const AutomaticIncrementalModal=mongoose.models.AutoIncreimentId||


mongoose.model('AutoIncreimentId',mongooseSch,'AutoIncreimentId');
export default AutomaticIncrementalModal;